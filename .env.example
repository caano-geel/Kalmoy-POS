# Environment variables (examples only — never commit real passwords)
# Prefer config.local.php (local) and config.production.php (InfinityFree).
# Optional: copy values below into a private `.env` — never commit `.env`

# APP_ENV=local
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=ash_pos_db
# DB_USER=root
# DB_PASSWORD=

# Optional: force canonical site URL
# APP_URL=https://kalmoypos.infinityfreeapp.com/

# ---------------------------------------------------------------------------
# InfinityFree — prefer htdocs/config.production.php (gitignored).
# Optional .env.infinityfree is also supported but not recommended.
# ---------------------------------------------------------------------------
# APP_ENV=production
# DB_HOST=sql110.infinityfree.com
# DB_PORT=3306
# DB_NAME=if0_42375362_kalmoyposdb
# DB_USER=if0_42375362
# DB_PASSWORD=PLACE_REAL_INFINITYFREE_PASSWORD_HERE
# DB_SSL=false
